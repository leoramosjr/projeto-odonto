import {
    Flex,
} from '@chakra-ui/react'

export default function Validation({
    setSection
} : {
    setSection: Function
}) {
    return (
        <Flex>
            Validation
        </Flex>
    )
}