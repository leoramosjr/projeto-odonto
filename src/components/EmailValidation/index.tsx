import {
    Flex,
} from '@chakra-ui/react'

export default function EmailValidation({
    setSection
} : {
    setSection: Function
}) {
    return (
        <Flex>
            Validation
        </Flex>
    )
}